{
  "name": "vue-langgraph-nestjs-concept",
  "version": "0.1.0",
  "private": true,
  "description": "Vue.js + LangGraph.js + NestJS AI chat todos app — concept project",
  "scripts": {
    "dev:backend": "pnpm --filter @todos/backend dev",
    "dev:frontend": "pnpm --filter @todos/frontend dev",
    "build:shared": "pnpm --filter @todos/shared build",
    "build:backend": "pnpm --filter @todos/backend build",
    "build:frontend": "pnpm --filter @todos/frontend build",
    "build": "pnpm build:shared && pnpm build:backend && pnpm build:frontend"
  },
  "engines": {
    "node": ">=18.0.0",
    "pnpm": ">=8.0.0"
  }
}
